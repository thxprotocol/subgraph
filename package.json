{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ thxprotocol/subgraph",
    "create-local": "graph create --node http://localhost:8020/ thxprotocol/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ thxprotocol/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 thxprotocol/subgraph --version-label v0.0.1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
